<%= simple_form_for(subject, defaults: { wrapper_html: { class: 'form-group' }, input_html: { class: 'form-control' } }) do |f| %>
  <%= f.input :name %>
  <%= f.input :code %>
  <%= f.input :num_units %>
  <hr>
  <%= f.submit "Save Subject" %>
<% end %>
